@model List<QuickStart.WebUI.Dtos.Sliders.ResultSliderDto>
@{
    ViewData["Title"] = "Slider YÃ¶netimi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Slider (Carousel) YÃ¶netimi</h4>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>BaÅŸlÄ±k</th>
                            <th>AÃ§Ä±klama</th>
                            <th>Resim Url</th>
                            <th>Durum</th>
                            <th>Sil</th>
                            <th>GÃ¼ncelle</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.SliderId</td>
                                <td>@item.Title</td>
                                <td>@item.Description</td>
                                <td><img src="@item.ImageUrl" alt="slider image" style="width:50px; height:50px;" /></td>
                                <td>@(item.IsActive ? "Aktif" : "Pasif")</td>
                                <td><a href="/Slider/Delete/@item.SliderId" class="btn btn-danger">Sil</a></td>
                                <td><a href="/Slider/Edit/@item.SliderId" class="btn btn-success">GÃ¼ncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/Slider/Create" class="btn btn-primary">Yeni Ekle</a>
            </div>
        </div>
    </div>
</div>
