@model List<QuickStart.WebUI.Dtos.FAQs.ResultFAQDto>
@{
    ViewData["Title"] = "SSS";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">SÄ±kÃ§a Sorulan Sorular (SSS) YÃ¶netimi</h4>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>SÄ±ra</th>
                            <th>Soru</th>
                            <th>Cevap</th>
                            <th>Durum</th>
                            <th>Sil</th>
                            <th>GÃ¼ncelle</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Order</td>
                                <td>@item.Question</td>
                                <td>@(item.Answer.Length > 50 ? item.Answer.Substring(0, 50) + "..." : item.Answer)</td>
                                <td>@(item.IsActive ? "Aktif" : "Pasif")</td>
                                <td><a href="/FAQ/Delete/@item.FAQId" class="btn btn-danger">Sil</a></td>
                                <td><a href="/FAQ/Edit/@item.FAQId" class="btn btn-success">GÃ¼ncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/FAQ/Create" class="btn btn-primary">Yeni Soru Ekle</a>
            </div>
        </div>
    </div>
</div>
